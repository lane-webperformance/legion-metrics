{
  "name": "legion-metrics",
  "version": "0.0.2",
  "description": "Generate samples and merge them into a metrics storage object. Part of the Legion load testing framework.",
  "main": "src/index.js",
  "scripts": {
    "test": "eslint ./src/** ./spec/** && jasmine && istanbul cover jasmine",
    "clean": "rimraf node_modules coverage"
  },
  "author": "Christopher Lane Hinson <lane@webperformance.com>",
  "license": "APSL-2.0",
  "dependencies": {
    "immutable": "^3.7.6",
    "ramda": "^0.21.0"
  },
  "devDependencies": {
    "eslint": "^2.10.2",
    "istanbul": "^0.4.2",
    "jasmine": "^2.4.1",
    "jasmine-immutablejs-matchers": "^0.1.2",
    "rimraf": "^2.5.2"
  }
}
